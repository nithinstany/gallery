<h1>Listing gallery_assets</h1>

<table>
  <tr>
  </tr>

<% @gallery_assets.each do |gallery_asset| %>
  <tr>
    <td><%= link_to 'Show', gallery_asset %></td>
    <td><%= link_to 'Edit', edit_gallery_asset_path(gallery_asset) %></td>
    <td><%= link_to 'Destroy', gallery_asset, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New gallery_asset', new_gallery_asset_path %>